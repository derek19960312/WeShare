<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="weshare.groupfour.derek">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CalendarActivity"
            android:label="@string/title_activity_calendar"
            android:theme="@style/AppTheme.Dialog"/>
        <activity android:name=".InsCourse.CourseCategoryActivity" />
        <activity android:name=".InsCourse.MyLikeCourseActivity" />
        <activity android:name=".InsCourse.MyCourseActivity" />
        <activity android:name=".InsCourse.InsCourseDetailActivity" />
        <activity android:name=".InsCourse.InsCourseBrowseActivity" />
        <activity android:name=".Goods.GoodsDetailActivity" />
        <activity android:name=".Goods.GoodsBrowseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>